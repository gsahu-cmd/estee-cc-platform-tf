# Root-level settings for scripts/process_json_request.py.
# Current implementation is validation-only. Keep Git disabled until file generation is implemented.
GIT_ENABLED=false
GIT_REMOTE_NAME=origin
GIT_BASE_BRANCH=main
GIT_BRANCH_PREFIX=json-request
GIT_COMMIT_MESSAGE_PREFIX=JSON request update
GIT_PR_ENABLED=false